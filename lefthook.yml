pre-commit:
  parallel: true
  jobs:
    - name: frontend linting
      run: bun lint
      glob: "*.{js,jsx,ts,tsx}"
    - name: backend linting
      root: src-tauri/
      glob: "*.rs"
      run: cargo fmt {files} & cargo clippy --fix
